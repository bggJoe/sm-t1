/**
 * Infrastructure layer of the Security module.
 * Contains: SecurityConfig (SecurityFilterChain bean), JwtAuthFilter, JwtTokenProvider impl.
 * Module-private — must not be accessed by other modules directly.
 */
package com.example.backend.security.infrastructure;
