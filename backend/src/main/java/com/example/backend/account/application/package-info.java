/**
 * Account module — application layer.
 * Contains: AccountApplicationService, AccountRepository (Port interface).
 * Must not depend on infrastructure layer (DEC-006, ARCH-RULE-002).
 */
package com.example.backend.account.application;
